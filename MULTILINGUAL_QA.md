# MULTILINGUAL QA

Generated: 2026-07-15

## Language Structure
- `ja`: root URL, default language, source pages remain in the project root.
- `zh-CN`: `/zh-cn/`
- `en`: `/en/`
- `ko`: `/ko/`
- `zh-TW`: `/zh-tw/`

## Generated Pages
Each language contains 13 pages:
`index.html`, `about.html`, `business.html`, `business-hire.html`, `business-taxi.html`, `business-auto.html`, `quality.html`, `works.html`, `company.html`, `news.html`, `contact.html`, `privacy.html`, `404.html`.

## Implementation Notes
- Static HTML pages are generated by `scripts/build-i18n.mjs`.
- Shared assets remain under `assets/css`, `assets/js`, `assets/images`, and `assets/videos`.
- No query parameter language routing is used.
- No frontend body-text swapping is used for translated pages.
- Header language switchers link to the corresponding page in each language.
- The language choice is saved to `localStorage` and Cookie only when the user clicks a language option. No forced redirect is implemented.
- Contact forms include a hidden `site_language` field.
- Japanese form labels were corrected to `航空便名` and `手荷物数`.
- `sitemap.xml` was regenerated with five-language URLs and hreflang alternates.

## Checks Performed
- `node --check assets/js/site.js`: passed.
- `node --check scripts/build-i18n.mjs`: passed.
- Generated language directory count: 13 HTML files in each of `/zh-cn/`, `/en/`, `/ko/`, `/zh-tw/`.
- HTTP smoke test on local server returned 200 for `/`, `/en/`, `/zh-cn/contact.html`, `/ko/contact.html`, `/zh-tw/news.html`.
- Language switcher markup exists on the tested pages.
- Visible Japanese kana residual check passed for generated zh-CN / en / ko / zh-TW pages, excluding intentional language option labels.

## Responsive Checks
A full browser viewport sweep at 320, 390, 768, 1024 and 1440px still needs manual visual confirmation. A Playwright runtime was not available as a project dependency in this environment, and the temporary `npx --package playwright` module was not exposed to the external script under the current Windows shell.

Recommended manual checks:
- Desktop header language switcher does not squeeze navigation.
- Mobile drawer shows the language switcher at the top.
- No horizontal overflow at 320px and 390px.
- English long titles and Korean line breaks remain readable.
- Contact form dynamic fields remain usable in all languages.

## Translation Review Status
The generated language pages are implementation drafts. They should be reviewed by native speakers before publication, especially for English, Korean, Simplified Chinese, Traditional Chinese, contact form labels and customer-facing status messages.

## Business Confirmation Required
- 約100台 vehicle network wording and counting basis.
- Osaka / Kyoto / Sakai / Minato office wording.
- License numbers and public display wording.
- G20, EXPO2025 and overseas brand event case wording.
- DiDi / Uber official links or whether to keep them as pending.
- News dates and publishability.
- Production canonical domain if different from `https://daitora-jp.com/`.
